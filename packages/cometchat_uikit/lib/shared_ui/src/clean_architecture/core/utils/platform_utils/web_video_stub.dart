import 'package:flutter/widgets.dart';

/// Native → null (playback goes through `video_player`).
Widget? buildWebVideoPlayer(String url) => null;

/// Native → null (the tray tile thumbnail uses `video_player` directly).
Widget? buildWebVideoThumb(String url) => null;
