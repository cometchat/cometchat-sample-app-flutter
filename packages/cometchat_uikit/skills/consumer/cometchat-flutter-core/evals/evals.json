{
  "skill_name": "cometchat-flutter-core",
  "evals": [
    {
      "id": 1,
      "prompt": "I'm setting up CometChat in my Flutter app. Here's my init code but it's not working, no error though:\n\nfinal settings = (UIKitSettingsBuilder()..appId = 'abc123'..region = 'us'..authKey = 'key123').build();\nawait CometChatUIKit.init(uiKitSettings: settings);\nawait CometChatUIKit.login('user1');\n\nUsers show as offline and typing indicators don't work. What's wrong?",
      "expected_output": "The response must identify that subscriptionType is missing from UIKitSettingsBuilder, which silently disables all presence and typing events.",
      "files": [],
      "assertions": [
        "Identifies missing subscriptionType as the root cause",
        "Shows the fix: ..subscriptionType = CometChatSubscriptionType.allUsers",
        "Mentions that omitting subscriptionType causes silent failure with no error"
      ]
    },
    {
      "id": 2,
      "prompt": "My chat screen has a weird bug where the message list jumps around when the keyboard opens. The messages shift up twice. Here's my screen:\n\nScaffold(\n  body: Column(\n    children: [\n      Expanded(child: CometChatMessageList(user: user)),\n      CometChatMessageComposer(user: user),\n    ],\n  ),\n)",
      "expected_output": "The response must identify that resizeToAvoidBottomInset defaults to true, causing double keyboard compensation since the composer handles keyboard spacing internally.",
      "files": [],
      "assertions": [
        "Identifies missing resizeToAvoidBottomInset: false as the cause",
        "Explains that CometChatMessageComposer handles keyboard spacing internally via SliverSpacing",
        "Shows the corrected Scaffold with resizeToAvoidBottomInset: false"
      ]
    },
    {
      "id": 3,
      "prompt": "I'm getting jank in my message list during keyboard animation. Build times are 50-80ms. I'm using CometChatThemeHelper.getColorPalette(context) in my custom bubble widget's build method. Is that related?",
      "expected_output": "The response must identify that calling CometChatThemeHelper in build() causes expensive InheritedWidget lookups on every rebuild during keyboard animation.",
      "files": [],
      "assertions": [
        "Identifies theme lookup in build() as the cause of jank",
        "Recommends caching in didChangeDependencies() with _themeInitialized flag",
        "Shows the correct hybrid caching pattern with code example",
        "Mentions that MediaQuery changes during keyboard animation trigger rebuilds"
      ]
    }
  ]
}