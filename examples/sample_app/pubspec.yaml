name: sample_app
description: "A sample app showcasing CometChat Flutter UIKit"
publish_to: 'none'
version: 6.0.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  intl: ^0.20.2
  shared_preferences: ^2.2.0
  permission_handler: ^11.3.1
  flutter_localizations:
    sdk: flutter
  cometchat_chat_uikit: ^6.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
