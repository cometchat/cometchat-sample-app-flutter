// Placeholder widget test. The stock counter-app template test was
// left over from `flutter create` and doesn't map to this app's
// widget tree. Replace with real tests as needed.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — replace with real tests', () {
    expect(1 + 1, 2);
  });
}
